MQ Journey Open Day QR Codes — 2026

Signing key ID: mqj-open-day-2026-02

Folder contents:
- SVG Masters: print-safe signed QR masters.
- PNG 2048px: lossless 2048 × 2048 pixel copies derived from the SVG masters.
- A4 Posters: labelled, print-ready PDF posters with location and map reference.
- Contact Sheet: A4 PDF and PNG containing all nine QR codes for verification.

Every location filename begins with its canonical display order and includes the
full location name and campus map reference. The QR quiet zone must remain clear.
Do not crop, recolour, overlay artwork, or add logos inside the QR boundary.

The private signing key is not included. These QR images, signatures, payloads,
posters, and checksums are public distribution material.

Verify the folder from its root with:

  shasum -a 256 -c SHA256SUMS

Physical print QA on a current Android phone and iPhone is still required before
event installation.
